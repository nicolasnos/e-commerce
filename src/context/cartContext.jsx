import React, {useState, createContext, useContext} from 'react'

const cartContext = () => {
  cartGeneralContext = createContext()
  const [items, setItems] = useState()

  const addItem = (item) =>{
    
  }

  return console.log(result)
}

export default cartContext